kind = "web"
previewPath = "/"
title = "Blue Planet Command Center"
version = "1.0.0"
id = "artifacts/blue-planet"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22365

[services.development]
run = "pnpm --filter @workspace/blue-planet run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/blue-planet", "run", "build" ]
publicDir = "artifacts/blue-planet/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22365"
BASE_PATH = "/"
